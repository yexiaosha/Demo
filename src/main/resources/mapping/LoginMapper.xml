<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yhdemo.demo.dao.LoginMapper">
    <select id="findUser" resultType="com.yhdemo.demo.pojo.User">
        SELECT id, username, email, gender, birthday
        FROM user_info
        WHERE username = #{username}
          AND password = #{password}
    </select>

    <select id="findUserByUsername" resultType="java.lang.String">
        SELECT username
        FROM user_info
        WHERE username = #{username}
    </select>
</mapper>